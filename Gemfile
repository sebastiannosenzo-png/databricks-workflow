source "https://rubygems.org"

# GitHub Pages-compatible local preview.
# On GitHub Pages itself the site builds via the remote_theme in _config.yml.
gem "jekyll", "~> 4.3"
gem "just-the-docs", "~> 0.10"

# Plugins
group :jekyll_plugins do
  gem "jekyll-remote-theme"
  gem "jekyll-seo-tag"
end

# Platform shims
gem "webrick", "~> 1.8"
